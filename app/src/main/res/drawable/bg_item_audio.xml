<?xml version="1.0" encoding="utf-8"?>
<!--
    Fondo de cada grabación de la lista. El estado se controla con
    setActivated(): así el adaptador nunca reemplaza el fondo en tiempo
    de ejecución, que es lo que borraba el padding del item.
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:state_activated="true">
        <shape android:shape="rectangle">
            <solid android:color="?attr/colorPrimaryContainer" />
            <corners android:radius="@dimen/radio_s" />
            <stroke android:width="1.5dp" android:color="?attr/colorPrimary" />
        </shape>
    </item>

    <item>
        <shape android:shape="rectangle">
            <solid android:color="?attr/colorSurface" />
            <corners android:radius="@dimen/radio_s" />
            <stroke android:width="1dp" android:color="?attr/colorOutlineVariant" />
        </shape>
    </item>

</selector>
